# logs / runtime artifacts
MUJOCO_LOG.TXT
*.log

# python
__pycache__/
*.pyc

# staging tarballs -- already extracted into assets/, safe to regenerate
staging_*.tar.gz

# intermediate CoACD output -- superseded by assets/objects/ organization
coacd_parts_out/

# reference-only clones, not part of our own codebase, not distributed
# with this repo (re-clone fresh on the pod if needed again)
third_party/oakink2_simenv/
third_party/regrind/

# training run outputs (large, regenerated by training itself)
runs/*/
dumps/*/
